# PR: Child 3 rollout hardening and cleanup plan

Closes #145

## What changed

- added `docs/job-id-migration-runbook.md` with:
  - canonical `job_id` / `job_setup_id` rules
  - remaining legacy `vacancy_id` surface
  - release A/B/C sequence
  - Neon validation flow and SQL checks
  - final cleanup gate
- updated release/sandbox docs to reference the `job_id` migration gate explicitly
- added regression coverage proving recruiter-facing communication plan markdown now links reports via `job_id`, not `vacancy_id`
- updated parent issue checklist in `docs/issues/2026-04-16-job-id-cutover-parent.md`

## Validation

- `node --test tests/integration/hiring-agent.test.js`
- `pnpm test:hiring-agent`
