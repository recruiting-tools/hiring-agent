## Summary
- make `job_setup` and `raw_job_setup_text` the canonical internal playbook/runtime context keys
- keep `vacancy` and `raw_vacancy_text` as compatibility aliases for persisted sessions and existing prompt templates
- update runtime docs and unit coverage for the new internal naming contract

## Testing
- `node --test tests/unit/playbook-step-handlers.test.js tests/unit/playbook-runtime-fallback.test.js`
- `pnpm test:hiring-agent`

## Issue
- closes #156
